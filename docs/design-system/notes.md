Notes on tokens and usage
- All pages should consume tokens from CSS variables in css/main.css
- For TI demo, extend with TI color tokens if needed (ti-demo.css)
- Accessibility: ensure color contrast and ARIA labels where needed
